VITE_API_URL=http://localhost:4000
# On Vercel leave empty (relative /api) — backend is same domain via vercel.json rewrites
# VITE_API_URL=

# Supabase primary backend (master_data.json now lives here, not PythonAnywhere)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Optional fallback during migration (leave empty to disable)
# PYTHONANYWHERE_URL=https://jestu93.pythonanywhere.com

